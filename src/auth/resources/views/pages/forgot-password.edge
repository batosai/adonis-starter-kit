@auth.layout()
  @jrmc.form({ action: route('auth.password.store'), error: false })
    @jrmc.form.control({ name: 'email', label: t('form.login.email') })
      @!jrmc.form.input({ type:'email', placeholder: 'john.doe@adonisjs.com', class: 'bg-base-200 w-full!' })
    @end

    @!jrmc.button({ text: t('form.login.validate'), class: 'w-full' })
  @end

  <div class="divider"></div>
  <a href="{{ route('auth.session.create') }}" class="text-sm link">{{ t('auth.session.loginLink') }}</a>
@end
